[package]
name = "snip-types"
version = "0.1.0"
edition = "2021"
description = "Shared types and error definitions for XDR Snip"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
